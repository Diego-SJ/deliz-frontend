import { Empty } from 'antd';

const EmptyChart = () => {
  return (
    <div className="flex justify-center items-center min-h-40 h-full">
      <Empty />
    </div>
  );
};

export default EmptyChart;
